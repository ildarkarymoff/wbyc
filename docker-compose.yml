version: "3"
services:
  web:
    build: .
    working_dir: /go/src
    restart: on-failure
    stdin_open: true
    command: go run /app/main.go
    env_file:
      - .env
    tty: true
    ports:
      - "8080:8080"
    links:
      - db
  db:
    image: redis
    ports:
      - "6379:6379"